{
  "__comment_model": "Model family to train.",
  "model": "OSS",
  "model_args": {
    "__comment_input_dim": "Input feature size per time step.",
    "input_dim": 1,
    "__comment_output_dim": "Latent output dimension before POD decode.",
    "output_dim": 256,
    "__comment_hidden_dim": "OSS latent channel width.",
    "hidden_dim": 256,
    "__comment_num_layers": "Number of stacked OSS cores.",
    "num_layers": 4,
    "__comment_discretization": "Time integrator type: IM or IMEX.",
    "discretization": "IMEX",
    "__comment_dt": "Base time-step initialization.",
    "dt": 1.0,
    "__comment_dt_min": "Lower bound for learned dt.",
    "dt_min": 0.001,
    "__comment_dt_max": "Upper bound for learned dt.",
    "dt_max": 1.0,
    "__comment_use_layernorm": "Apply LayerNorm before rollout.",
    "use_layernorm": 1,
    "__comment_residual_weight": "Residual mix across stacked OSS layers.",
    "residual_weight": 0.25,
    "__comment_proj_dropout": "Dropout on OSS in/out projections.",
    "proj_dropout": 0.0,
    "__comment_robust_dt_init": "Use clipped/logit-safe dt initialization.",
    "robust_dt_init": 1,

    "__comment_use_input_dt": "Option 1: token-wise adaptive dt from input.",
    "use_input_dt": 1,

    "__comment_use_input_drive_damping": "Option 2: input-conditioned drive and damping.",
    "use_input_drive_damping": 1,
    "__comment_input_drive_scale": "Option 2: scale of input-driven drive term.",
    "input_drive_scale": 0.02,
    "__comment_input_damping_scale": "Option 2: scale of input-driven damping term.",
    "input_damping_scale": 0.02,

    "__comment_use_d_skip": "Option 3: add D-skip path y_t = h_t + D*u_t.",
    "use_d_skip": 1,
    "__comment_d_skip_init": "Initial value for D-skip parameter.",
    "d_skip_init": 1.0,

    "__comment_use_osc_gate": "Option 4: oscillator gating branch (z-style).",
    "use_osc_gate": 1,

    "__comment_use_causal_prefilter": "Option 5: causal depthwise conv prefilter.",
    "use_causal_prefilter": 1,
    "__comment_prefilter_kernel_size": "Option 5: prefilter temporal kernel size.",
    "prefilter_kernel_size": 3,

    "__comment_use_expand_project": "Option 6: expand oscillator latent space then project back.",
    "use_expand_project": 1,
    "__comment_expand_factor": "Option 6: expansion factor for oscillator latent core.",
    "expand_factor": 2,
    "__comment_expand_init_scale": "Option 6: init scale for expand/project layers.",
    "expand_init_scale": 0.02,

    "__comment_use_coupled_oscillators": "Option 7: low-rank coupled oscillator operators.",
    "use_coupled_oscillators": 0,
    "__comment_coupling_rank": "Option 7: low-rank dimension for coupling operators.",
    "coupling_rank": 4,
    "__comment_coupling_scale": "Option 7: strength of coupled oscillator contribution.",
    "coupling_scale": 0.05
  },
  "training": {
    "__comment_num_epochs": "Total training epochs.",
    "num_epochs": 1000,
    "__comment_batch_size": "Mini-batch size.",
    "batch_size": 16,
    "__comment_lr": "Optimizer learning rate.",
    "lr": 0.001,
    "__comment_eval_interval": "Epochs between eval logs.",
    "eval_interval": 5
  },
  "data": {
    "__comment_grid_x": "Spatial grid size.",
    "grid_x": 32,
    "__comment_grid_t": "Temporal grid size.",
    "grid_t": 50,
    "__comment_n_components_encode": "POD encode component count.",
    "n_components_encode": 256,
    "__comment_n_components_decode": "POD decode component count.",
    "n_components_decode": 256,
    "__comment_T": "Terminal simulation time.",
    "T": 1
  },
  "__comment_seed": "Random seed for reproducibility.",
  "seed": 0
}
